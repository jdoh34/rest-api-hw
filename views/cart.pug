extends layout

block title
  | Shopping Cart

block content
  h1 Shopping Cart
  
  if cartItems && cartItems.length
    if user
      .discount-code
        span 🎉 WELCOME DISCOUNT 🎉
        .code GATOR-#{Math.floor(Math.random() * 90000) + 10000}
        p Use this code at checkout for 20% OFF your first order!
    
    table.cart-table
      thead
        tr
          th Item
          th Price
          th Quantity
          th Subtotal
          th
      tbody
        each item in cartItems
          tr
            td= item.name
            td $#{item.price.toFixed(2)}
            td= item.quantity
            td $#{(item.price * item.quantity).toFixed(2)}
            td
              form(action="/cart/remove", method="POST")
                input(type="hidden", name="name", value=item.name)
                button.remove-btn(type="submit") Remove

    p.cart-total Total: $#{total}

    div(style="display:flex;justify-content:space-between;align-items:center;margin-top:1.5rem;flex-wrap:wrap;gap:1rem;")
      a(href="/products", style="background:#f1f5f9;color:#1e293b;padding:10px 20px;border-radius:8px;text-decoration:none;font-weight:500;") ← Keep Shopping
      form(action="/checkout", method="GET", style="margin:0;display:flex;")
        button(type="submit", style="background:#22c55e;color:white;padding:10px 24px;border:none;border-radius:8px;font-size:1rem;font-weight:600;cursor:pointer;") Proceed to Checkout →
  else
    .empty-cart
      p Your cart is empty.
      a.btn-primary(href="/products") Browse Services →
